[project]
name = "qweather-mcp-server"
version = "0.7.0"
description = "和风天气 MCP 服务 - 基于 Model Context Protocol 的天气数据查询服务"
readme = "README.md"
license = { text = "GPL-3.0" }
authors = [
    { name = "fengyucn", email = "your-email@example.com" }
]
keywords = ["weather", "mcp", "hefeng", "qweather", "climate"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Atmospheric Science",
    "Topic :: Internet :: WWW/HTTP :: Dynamic Content",
]
requires-python = ">=3.11"
dependencies = [
    "cryptography>=41.0.0",
    "httpx>=0.25.0",
    "mcp[cli]>=1.10.0",
    "pyjwt>=2.8.0",
    "python-dotenv>=1.0.0",
    "typer>=0.12.0",
]

[project.urls]
Homepage = "https://github.com/ptbsare/qweather-mcp-server"
Repository = "https://github.com/ptbsare/qweather-mcp-server.git"
Issues = "https://github.com/ptbsare/qweather-mcp-server/issues"
Documentation = "https://github.com/ptbsare/qweather-mcp-server#readme"

[project.scripts]
qweather-mcp-server = "qweather_mcp_server.main:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.uv]
package = true

[dependency-groups]
dev = [
    "build>=1.3.0",
    "mypy>=1.17.0",
    "ruff>=0.12.4",
    "twine>=6.2.0",
]
